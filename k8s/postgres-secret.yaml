apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: default
type: Opaque
stringData:
  # IMPORTANT: Change these credentials in production!
  # Use a secrets management system (Vault, sealed-secrets, etc.)
  POSTGRES_USER: n8n
  POSTGRES_PASSWORD: n8n_secure_password_change_me
  POSTGRES_DB: n8n
